{
  "name": "cloudflare-gemini",
  "version": "1.0.0",
  "description": "Google Gemini API proxy service for Cloudflare Workers",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@google/genai": "^1.6.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.8.3"
  }
}
